{
  "name": "poker-ledger-payout",
  "version": "1.0.0",
  "description": "Scrape poker ledger and calculate payouts",
  "main": "scraper.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm start",
    "postinstall": "npm install puppeteer && node install-chromium.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "puppeteer": "^10.0.0"
  }
}
